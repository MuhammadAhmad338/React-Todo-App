{
  "name": "react_practice_project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server & npm install) || (cd client && npm install)",
    "server": "node ./serverSide/server.js",
    "client": "npm start && cd clientSide"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
